#simple intrest
#formula PTR/100 , P = PRINCIPLE, T = TIME, R= RATE OF INTREST
def intrest(p,t,r):
    # p = int(p)
    # t = int(t)
    # r = int(r)
    #ptr = p*t*r
    intrest_amount = (p*t*r)/100
    return intrest_amount
print (intrest(100000,1,2))







